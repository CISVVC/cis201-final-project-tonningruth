#ifndef LIBRARIES_H
#define LIBRARIES_H
#include <iostream>
#include <string>
#include <sstream>
#include <vector>
#include <algorithm>
#include <stdlib.h>
#include <fstream>
const int PUNC_SIZE = 16; // The number of items held in 'PUNC_ARRAY'
const char PUNC_ARRAY[] = {'[', ']', ',', '.', '?', '\'', '"', '(', ')', ';', ':', '!', '-', '\n', '\r', '\t'};
#include "word.h"
#include "concord.h"
#endif
